import { useNavigate } from "react-router-dom";
import { Controller, useForm } from "react-hook-form";
import React from "react";
const Login = () => {
  return (
    <div className="mainLogin">
      <div className="wrapperlogin">login</div>
    </div>
  );
};

export default Login;
